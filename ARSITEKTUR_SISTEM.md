# 🏗️ Arsitektur Sistem SIBUSKU

## 📊 Diagram Arsitektur

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Browser    │  │   Mobile     │  │   API Client │          │
│  │  (Blade UI)  │  │   Browser    │  │   (Future)   │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
└─────────┼──────────────────┼──────────────────┼─────────────────┘
          │                  │                  │
          └──────────────────┼──────────────────┘
                             │
┌────────────────────────────┼────────────────────────────────────┐
│                    PRESENTATION LAYER                            │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Laravel Routes (web.php)                     │   │
│  │  • Public Routes    • Auth Routes   • Admin Routes       │   │
│  └───────────────────────────┬──────────────────────────────┘   │
│                              │                                    │
│  ┌───────────────────────────▼──────────────────────────────┐   │
│  │              Controllers (HTTP Layer)                    │   │
│  │  • BookingController  • TripController  • BusController  │   │
│  │  • ProfileController  • SeatController                   │   │
│  └───────────────────────────┬──────────────────────────────┘   │
│                              │                                    │
│  ┌───────────────────────────▼──────────────────────────────┐   │
│  │              Middleware                                    │   │
│  │  • auth  • admin  • force.phone  • throttle              │   │
│  └───────────────────────────┬──────────────────────────────┘   │
└───────────────────────────────┼──────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                      BUSINESS LOGIC LAYER                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Services (Business Logic)                    │   │
│  │  ┌──────────────────┐  ┌──────────────────┐            │   │
│  │  │ BookingService   │  │ SeatService      │            │   │
│  │  │ • createBooking  │  │ • validateSeats  │            │   │
│  │  │ • updateStatus   │  │ • assignSeats    │            │   │
│  │  │ • cancelBooking  │  │ • getAvailability│            │   │
│  │  └──────────────────┘  └──────────────────┘            │   │
│  │  ┌──────────────────┐  ┌──────────────────┐            │   │
│  │  │ WhatsAppService  │  │ SeatRecommendation│            │   │
│  │  │ • send           │  │ • recommendSeats  │            │   │
│  │  │ • notifyCreated  │  └──────────────────┘            │   │
│  │  │ • notifyConfirmed│                                   │   │
│  │  └──────────────────┘                                   │   │
│  └───────────────────────────┬──────────────────────────────┘   │
│                              │                                    │
│  ┌───────────────────────────▼──────────────────────────────┐   │
│  │              Events & Listeners                          │   │
│  │  ┌──────────────────┐  ┌──────────────────┐            │   │
│  │  │ BookingCreated   │  │ BookingStatus    │            │   │
│  │  │      ↓           │  │    Updated       │            │   │
│  │  │ SendBooking      │  │      ↓           │            │   │
│  │  │ Notification     │  │ LogBookingStatus │            │   │
│  │  └──────────────────┘  └──────────────────┘            │   │
│  └───────────────────────────┬──────────────────────────────┘   │
│                              │                                    │
│  ┌───────────────────────────▼──────────────────────────────┐   │
│  │              Policies (Authorization)                    │   │
│  │  • TripPolicy  • BookingPolicy  • BusPolicy             │   │
│  └───────────────────────────┬──────────────────────────────┘   │
└───────────────────────────────┼──────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                        DATA LAYER                                │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Eloquent Models                              │   │
│  │  • User  • Bus  • Route  • Trip  • Booking               │   │
│  │  • BookingSeat  • BusSeat  • BookingStatusLog            │   │
│  │  • WhatsAppLog                                            │   │
│  └───────────────────────────┬──────────────────────────────┘   │
│                              │                                    │
│  ┌───────────────────────────▼──────────────────────────────┐   │
│  │              Database (MySQL/PostgreSQL)                  │   │
│  │  • users  • buses  • routes  • trips  • bookings        │   │
│  │  • booking_seats  • bus_seats  • booking_status_logs    │   │
│  │  • whatsapp_logs                                         │   │
│  └──────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                    EXTERNAL SERVICES                              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  • Fonnte API (WhatsApp Notifications)                   │   │
│  │  • PDF Generator (DomPDF)                                │   │
│  └──────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flow Booking Process

```
1. USER ACTION
   └─> Pilih Trip & Kursi
       └─> Submit Form Booking
           │
2. CONTROLLER
   └─> BookingController@store
       │
       ├─> Validasi (StoreBookingRequest)
       │
       └─> BookingService::createBooking()
           │
           ├─> SeatService::validateSeats()
           │   └─> Cek kursi tersedia
           │   └─> Cek tidak conflict
           │
           ├─> DB::transaction()
           │   ├─> Trip::lockForUpdate()
           │   ├─> Booking::create()
           │   ├─> BookingSeat::create()
           │   └─> Trip::decrement('available_seats')
           │
           └─> Event::dispatch(BookingCreated)
               │
3. EVENT LISTENER
   └─> SendBookingNotification
       └─> WhatsAppService::notifyBookingCreated()
           └─> Kirim WA ke User & Admin
           └─> Log ke whatsapp_logs
           │
4. RESPONSE
   └─> Redirect ke Success Page
       └─> Tampilkan Kode Booking
       └─> Tampilkan Status WA
```

---

## 🔐 Security Architecture

### Authentication & Authorization Flow

```
┌─────────────┐
│   Request   │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│  Auth Middleware│  ← Cek user login?
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Admin Middleware│  ← Cek role = admin?
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  Policy Check   │  ← authorize('action', Model)
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│   Controller    │
└─────────────────┘
```

### Authorization Layers
1. **Route Middleware** - `auth`, `admin`
2. **Policy** - Fine-grained permission
3. **Service Layer** - Business logic validation

---

## 📦 Service Layer Pattern

### BookingService
```php
┌─────────────────────────────────────┐
│         BookingService              │
├─────────────────────────────────────┤
│ • createBooking()                   │
│   ├─> Validasi via SeatService      │
│   ├─> DB Transaction                │
│   ├─> Lock Trip (lockForUpdate)     │
│   ├─> Create Booking                │
│   ├─> Assign Seats                  │
│   └─> Dispatch Event                │
│                                     │
│ • updateBookingStatus()             │
│   ├─> Update Status                 │
│   ├─> Dispatch Event                │
│   └─> Release Seats (if cancelled)  │
│                                     │
│ • cancelBookingByUser()             │
│   ├─> Check Authorization           │
│   ├─> Check Status (pending only)   │
│   └─> Call updateBookingStatus()    │
└─────────────────────────────────────┘
```

### SeatService
```php
┌─────────────────────────────────────┐
│         SeatService                 │
├─────────────────────────────────────┤
│ • validateSeats()                   │
│   ├─> Cek duplikat                 │
│   ├─> Cek jumlah tersedia          │
│   └─> Cek kursi sudah dibooking   │
│                                     │
│ • assignSeatsToBooking()            │
│   └─> Create BookingSeat records   │
│                                     │
│ • getSeatAvailability()             │
│   └─> Return status per kursi      │
└─────────────────────────────────────┘
```

---

## 🎯 Event-Driven Architecture

### Event Flow
```
┌──────────────────┐
│  Booking Created │
└────────┬─────────┘
         │
         ▼
┌─────────────────────────┐
│  EventServiceProvider   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│  SendBookingNotification│
│  (Listener)             │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│  WhatsAppService        │
│  • Send to User         │
│  • Send to Admin        │
│  • Log to Database      │
└─────────────────────────┘
```

### Events & Listeners
- **BookingCreated** → `SendBookingNotification`
- **BookingStatusUpdated** → `LogBookingStatusChange`

---

## 🗄️ Database Schema

### Core Tables
```
users
  ├── bookings (1:N)
  └── booking_status_logs (1:N)

buses
  ├── trips (1:N)
  └── bus_seats (1:N)

routes
  └── trips (1:N)

trips
  ├── bookings (1:N)
  └── booking_seats (1:N)

bookings
  ├── booking_seats (1:N)
  ├── booking_status_logs (1:N)
  └── whatsapp_logs (1:1 latest)
```

### Key Constraints
- `unique_trip_seat` - Satu kursi tidak bisa dibooking 2x untuk trip yang sama
- `unique_bus_seat` - Satu bus tidak bisa punya 2 kursi dengan nomor sama
- Foreign keys dengan cascade delete

---

## 🔄 Concurrency Control

### Booking dengan Lock
```php
DB::transaction(function () {
    $trip = Trip::lockForUpdate()->find($id);
    // Validasi kursi
    // Create booking
    // Update available_seats
});
```

**Manfaat:**
- Mencegah race condition
- Atomic operation
- Data consistency

---

## 📱 API Endpoints

### Public
- `GET /` - Home
- `GET /search` - Form pencarian
- `POST /search` - Cari trip
- `GET /trips/{trip}` - Detail trip

### Authenticated
- `GET /api/trips/{trip}/seats` - Status kursi real-time
- `GET /api/trips/{trip}/seats/recommend` - Rekomendasi kursi
- `POST /bookings` - Create booking (rate limited)
- `GET /bookings/{booking}/success` - Success page
- `GET /bookings/{booking}/ticket` - Download PDF
- `POST /bookings/{booking}/cancel` - Cancel booking

### Admin
- `GET /admin/*` - Semua route admin (protected)

---

## 🎨 Frontend Architecture

### Component Structure
```
resources/views/
├── components/
│   ├── form/          # Form components
│   ├── ui/            # UI components
│   └── ...
├── layouts/           # Layout templates
└── [feature]/         # Feature views
```

### JavaScript (Alpine.js)
- **Toast Store** - Global notification system
- **Modal Animations** - Smooth transitions
- **Form Validation** - Client-side validation

---

## 🔧 Key Technologies

- **Backend:** Laravel 11, PHP 8.2+
- **Frontend:** Blade, Tailwind CSS, Alpine.js
- **Database:** MySQL/PostgreSQL
- **External:** Fonnte API (WhatsApp), DomPDF (PDF)
- **Testing:** Pest PHP

---

## 📈 Scalability Considerations

### Current Architecture Supports:
- ✅ Horizontal scaling (stateless)
- ✅ Database connection pooling
- ✅ Caching (bisa ditambahkan Redis)
- ✅ Queue jobs (bisa ditambahkan untuk WA)

### Future Improvements:
- Queue untuk WhatsApp notifications
- Redis untuk caching
- API versioning
- Microservices (jika diperlukan)

---

**Arsitektur ini dirancang untuk:**
- ✅ Maintainability
- ✅ Scalability
- ✅ Security
- ✅ Testability
- ✅ Performance

